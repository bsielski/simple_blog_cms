<div>
  <article class="article">
    <header class="article_header">
      <h1 class="article_header__title"><%= link_to article.title, article %></h1>
      <footer>
	<p class="article_header__tags">
	  <%= pluralize(article.categories.count, "Category") %>:
	  <% article.categories.each do |category| %>
	    <% if category.id == article.categories.last.id %>
	      <%= link_to category.name, category_articles_path(category) %>
	    <% else %>
	      <%= link_to category.name, category_articles_path(category) %>,
	    <% end %>
	  <% end %>
	</p>
      </footer>
    </header>
    
    <div class="js_markdown article_body"><%= raw article.content %></div>

    <footer class="article_footer">
      <p>at: <%= article.created_at %></p>
      <p>by: <%= article.author.name %></p>
    </footer>
  </article>
  
</div>
